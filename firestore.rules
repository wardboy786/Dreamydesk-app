rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access for development.
    // WARNING: This is insecure for production apps.
    // Restrict access with authentication before deploying.
    match /{path=**} {
      allow read, write;
    }
  }
}
